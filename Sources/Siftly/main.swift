import SwiftUI
import SiftlyKit

// Executable host. All real functionality lives in the SiftlyKit library so it
// can be unit tested and, eventually, hosted by a different platform shell.
SiftlyApp.main()
